# inkscape notes (fill me in when you start fussing around again)

Reminder file. The reconstructed-from-docs version lives in
`plotterpi/WORKFLOW.md` — this file is for what actually happens at the
machine, written down THIS time.

## quick recall (from docs, unverified by hands)

- Extension: Extensions > AxiDraw > AxiDraw Control (or Bantam Tools submenu).
  Each tab = an action, Apply fires it.
- Offline dry run, no plotter needed: Options tab → "Preview mode rendering" =
  All movement + check "Report time elapsed" → Plot tab → Apply.
  Cleanup: Manual tab → "Strip plotter data".
- Layers tab → "Plot only layers beginning with" N = one pen at a time.
- Setup tab → pen height sliders + "Raise pen, turn off motors" for hand-homing.

## TODO when the plotter comes back out — capture for real:

- [ ] which extension is actually installed on the laptop (Bantam NextDraw or
      legacy AxiDraw) + version, and the exact menu path
- [ ] model config: where I set AxiDraw V3/A3 in the dialog
- [ ] pen heights that work: UP ___ %  DOWN ___ %  (per pen type?)
- [ ] speed setting I actually use: ___ %
- [ ] my real layer prep steps in Inkscape (naming, deleting, ordering)
- [ ] preview/time-estimate: does it match reality? fudge factor: ___
- [ ] homing ritual, paper alignment + taping order
- [ ] pen swap steps between layers without wrecking registration
- [ ] anything that went wrong + fix

## session log

<!-- date: what I did, what worked, what to remember -->
